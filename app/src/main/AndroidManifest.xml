<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appoderfood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DangNhapActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".TrangChuActivity"></activity>
        <activity android:name=".ThemBanAnActivity"
            android:theme="@style/PopUpActivity"></activity>
        <activity android:name=".ThemThucDonActivity"></activity>
        <activity android:name=".ThemLoaiThucDonActivity"
            android:theme="@style/PopUpActivity"
            android:label="@string/loaithucdon"></activity>
    </application>

</manifest>